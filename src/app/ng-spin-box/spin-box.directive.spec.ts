import { SpinBoxDirective } from './spin-box.directive';

describe('SpinBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new SpinBoxDirective();
    expect(directive).toBeTruthy();
  });
});
